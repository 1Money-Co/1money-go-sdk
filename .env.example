# =============================================================================
# 1Money Go SDK Configuration
# =============================================================================
# Copy this file to .env and fill in your credentials.
# Never commit your actual .env file to version control.
# =============================================================================

# -----------------------------------------------------------------------------
# Authentication (Required)
# -----------------------------------------------------------------------------
# Your 1Money API credentials. Obtain these from the 1Money dashboard.
ONEMONEY_ACCESS_KEY=<YOUR_ONEMONEY_ACCESS_KEY>
ONEMONEY_SECRET_KEY=<YOUR_ONEMONEY_SECRET_KEY>

# -----------------------------------------------------------------------------
# API Endpoint
# -----------------------------------------------------------------------------
# Base URL for the 1Money API.
# - Production: https://api.1money.com
# - Sandbox:    https://api.sandbox.1money.com (default if not set)
ONEMONEY_BASE_URL=https://api.sandbox.1money.com

# -----------------------------------------------------------------------------
# Environment Mode
# -----------------------------------------------------------------------------
# Enable sandbox mode for testing without real transactions.
# Set to 1 to enable, 0 or omit to disable.
ONEMONEY_SANDBOX=1

# -----------------------------------------------------------------------------
# Debug & Logging
# -----------------------------------------------------------------------------
# Debug mode: When set to 1, automatically adds X-Forwarded-For header with local IP.
# This is useful for testing IP-based rate limiting and other IP-dependent features.
# ONEMONEY_DEBUG=1

# Include curl command in debug output (useful for reproducing requests manually).
# ONEMONEY_DEBUG_CURL=1

# Generate request files: When set to 1, saves each request body to output/ directory
# as JSON files. File name format: {METHOD}_{path}_{timestamp}.json
# ONEMONEY_GEN_REQ=1

# Log level: debug, info, warn, error (default: info)
# ONEMONEY_LOG_LEVEL=debug

# Enable stack traces in error logs (can be verbose).
# ONEMONEY_ENABLE_STACKTRACE=false

# -----------------------------------------------------------------------------
# Testing
# -----------------------------------------------------------------------------
# Customer ID for running examples (required for most example scripts).
# ONEMONEY_CUSTOMER_ID=<YOUR_CUSTOMER_ID>

# Wallet address for withdrawal tests (e.g., Polygon USDC address).
ONEMONEY_TEST_WALLET_ADDRESS=""